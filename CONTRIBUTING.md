# Contributing

1. Keep the MVP small. Prefer a sharp SPEC change over a large feature.
2. `go test ./...` and `go build ./cmd/worlds` must pass.
3. Do not commit secrets or personal vault content. Use `example-vault/` only.
4. Match the house style: simple words, plain hyphens, short docs.
